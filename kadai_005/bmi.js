// 定数の宣言・値の代入
const weight = 68;

// コンソールへの出力
console.log(weight);

// 値の再代入
const height = 1.7;

// コンソールへの出力
console.log(height);

// 値の再代入
const bmi=(68/(1.7*1.7));

// コンソールへの出力
console.log(bmi);